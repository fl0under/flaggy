version = "1.0"
artifacts = ["/logs/agent/trajectory.json"]

[task]
name = "flaggy/ctf-supersanic-rsa"
description = "Solve the local supeRSAnic RSA CTF challenge. Inspect the provided Python challenge, recover the generated 6-digit PIN from the printed RSA values using safe local analysis, and submit the PIN to obtain the flag. Write the final flag and su"
keywords = ["flaggy", "security", "ctf"]

[metadata]
author_name = "Flaggy"
author_email = "security@example.invalid"
category = "security"
difficulty_explanation = "Generated from a Flaggy scoped task. The verifier scores report/evidence quality and scope discipline."

[environment]
network_mode = "no-network"
build_timeout_sec = 600.0
cpus = 2
memory_mb = 4096
storage_mb = 10240

[agent]
timeout_sec = 1800.0
network_mode = "no-network"


[verifier]
timeout_sec = 180.0
network_mode = "no-network"
