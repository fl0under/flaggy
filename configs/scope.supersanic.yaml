program: ctf-practice
policy_url: null
mode: ctf
allowed_targets:
  - name: supersanic
    kind: binary
    path: ./work/attempt_468/supersanic1.py
    notes: "Local supeRSAnic RSA CTF challenge recovered from the previous attempt. Analyze locally only."
allowed_networks:
  - "127.0.0.1/32"
  - "localhost"
forbidden_actions:
  - denial_of_service
  - persistence
  - stealth
  - data_exfiltration
  - destructive_changes
reporting:
  out_dir: runs
  evidence_dir: evidence
